log('hello');

var that = this;

for (var key in that) {
  //log(key);
}

//log(point_zero.x);
//point_zero.x = 199;
//point_zero.y = 42;
//log(point_zero.x);
log(palette.point.x);
log(palette.int_array[0]);
log(palette.points_array[1].x);
palette.points_array[1].x = 10;
log(palette.points_array[1].x);
